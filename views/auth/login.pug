extends ../layout/index
block content
    .py-10
        h1(class="text-4xl my-10 font-extrabold text-center") Bienes 
            span(class="font-normal") Raíces
        h2(class="text-center text-2xl font-extrabold") #{title}

        div(class="mt-8 mx-auto max-w-md")
            div(class="bg-white py-8 px-4 shadow")
                form(class="space-y-6")
                    div
                        label(for="email" class="label") Email
                        input(type="email" id="email" email="email" class="input" placeholder="Tu email")
                    div
                        label(for="password" class="label") Password
                        input(type="password" id="password" password="password" class="input" placeholder="Tu password")
                    
                    input(type="submit" value="Iniciar Sesión" class="btn")
                div(class="content-link")
                    a(href="/auth/registro" class="link-action") ¿Aún no tienes una cuenta? Registrate
                    a(href="/auth/recuperar-password" class="link-action") Olvide mi contraseña