extends ../layout/index
block content
    .py-10
        h1(class="text-4xl my-10 font-extrabold text-center") Bienes 
            span(class="font-normal") Raíces

        if errors 
            div(class="alert-error")
                each error in errors
                    p(class="alert-error__text") #{error.msg}

        div(class="mt-8 mx-auto max-w-md")
            div(class="bg-white py-8 px-4 shadow")
                form(class="space-y-6" method="POST" action="/auth/registro")
                    div
                        label(for="name" class="label") Nombre
                        input(type="text" id="name" name="name" class="input" placeholder="Tu nombre" value=name || "")
                    div
                        label(for="email" class="label") Email
                        input(type="email" id="email" name="email" class="input" placeholder="Tu email" value =email || "")
                    div
                        label(for="password" class="label") Password
                        input(type="password" id="password" name="password" class="input" placeholder="Tu password" value=password || "")
                    div
                        label(for="password2" class="label") Repetir password
                        input(type="password" id="password2" name="password2" class="input" placeholder="Repetir password" value=password2 || "")
                    
                    input(type="submit" value="Crear cuenta" class="btn")
                div(class="content-link")
                    a(href="/auth/login" class="link-action") ¿Ya tienes una cuenta? Inicia Sesión
                    a(href="/auth/recuperar-password" class="link-action") Olvide mi contraseña